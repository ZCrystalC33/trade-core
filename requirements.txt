# Trade Core — Python 依賴套件
# 安裝：pip install -r requirements.txt

# ── 核心依賴 ────────────────────────────────────
requests>=2.28.0          # HTTP 請求（FinMind API、CoinGecko API）
pandas>=1.5.0             # 數據處理
numpy>=1.23.0             # 數值計算
python-dotenv>=1.0.0      # 環境變數管理（~/.trade_core.env）

# ── 可視化 ─────────────────────────────────────
matplotlib>=3.0.0          # 儀表板繪製
mplfinance>=0.12.0         # K線圖繪製

# ── 美股資料 ─────────────────────────────────────
yfinance>=0.2.0            # Yahoo Finance 美股攝取

# ── 可選：有Talib可用但非必要 ─────────────────────
# TA-Lib 需要另外安裝（版本相依性強，慎選）
# pandas_ta>=0.3.0          # 輕量技術指標替代（可選）

# ── 可選：回測框架 ───────────────────────────────
# backtesting>=0.2.0        # 簡單回測（目前用自建引擎）
# backtrader>=1.9.0         # 完整回測框架（未來擴充）
